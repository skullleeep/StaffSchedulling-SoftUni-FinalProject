@using StaffScheduling.Web.Models.InputModels.Vacation;
@model AddVacationOfEmployeeInputModel

@* <form asp-controller="Vacation" asp-action="AddVacationOfEmployee" method="post" class="col-md-8  d-flex gap-2">
    <input asp-for="CompanyId" type="hidden" readonly/>
    <input asp-for="EmployeeId" type="hidden" readonly />

    <label asp-for="EndDate" class="form-label">End Date</label>
    <input asp-for="StartDate" class="form-control w-75 flex-grow-1" placeholder="Enter employee email" required />
    <button type="submit" class="btn btn-success w-auto">Add Employee</button>
</form> *@

<form asp-controller="Vacation" asp-action="AddVacation" method="post">
    <input asp-for="CompanyId" type="hidden" readonly />
    <input asp-for="EmployeeId" type="hidden" readonly />

    <div class="row g-3 align-items-center">
        <div class="col-md-5">
            <label asp-for="StartDate" class="form-label">Start Date</label>
            <input asp-for="StartDate" type="date" class="form-control" required />
        </div>

        <div class="col-md-5">
            <label asp-for="EndDate" class="form-label">End Date</label>
            <input asp-for="EndDate" type="date" class="form-control" required />
        </div>

        <div class="col-md-2 d-grid">
            <button type="submit" class="btn btn-success w-auto">Add Vacation</button>
        </div>
    </div>
</form>